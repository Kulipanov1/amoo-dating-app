[build]
  command = "npm ci && npm run build"
  publish = "web-build"

[context.production]
  command = "npm ci && npm run build"

[context.develop]
  command = "npm ci && npm run build"

[context.branch-deploy]
  command = "npm ci && npm run build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"
  CI = "false" 