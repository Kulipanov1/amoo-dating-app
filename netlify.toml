[build]
  command = "npm install && NODE_OPTIONS=--max_old_space_size=4096 npx expo export -p web"
  publish = "web-build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"
  CI = "false" 